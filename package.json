{
  "name": "payment-app",
  "version": "1.0.0",
  "private": true,
  "description": "Test e-commerce checkout service (Stripe + MongoDB) used to reproduce a stuck-order-status bug",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.8.0",
    "stripe": "^16.8.0"
  }
}
